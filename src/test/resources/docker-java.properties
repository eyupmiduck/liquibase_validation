# Testcontainers 2.0.5 uses docker-java, whose client defaults to Docker API
# 1.32. Docker 29 rejects API versions below 1.40, so pin the lowest accepted
# version (Docker 29's minimum). Remove once docker-java defaults to >= 1.40.
api.version=1.40
