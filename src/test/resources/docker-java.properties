# Testcontainers (<= 1.21.3) shades docker-java, which defaults to Docker
# API 1.32. Docker 29 requires API >= 1.40, so pin a newer client version.
# Remove once Testcontainers supports Docker 29+ natively.
api.version=1.44
